services:
  - type: web
    name: process-identification-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_main:app --host 0.0.0.0 --port $PORT
